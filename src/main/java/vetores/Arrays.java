package vetores;

public class Arrays {
    public static void main(String[] args) {
        int[] numeros = {9, 10, 12, 25, 2};
        int maior = numeros[0];
        int menor = numeros[0];
        int media;

        for (int i=0; i < numeros.length; i++) {
            if (numeros[i] > maior) {

            }
        }
    }
}
